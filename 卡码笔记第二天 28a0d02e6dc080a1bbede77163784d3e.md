# 卡码笔记第二天

LC209长度最小的子数组**Minimum Size Subarray Sum**

视频：

1.for循环里的j指向终止位置*这是关键

2.如何移动起始位置* 是关键

当收集的和大于等于target，向后移动一位j-i +1

向后移动一位，窗口收集的和 把起始位置减掉：window -= nums[i]

代码：

while window >= target:
    res = min(res, right - left + 1)
    window -= nums[left]
    left += 1

LC59螺旋矩阵|| **Spiral Matrix II**

视频：统一左闭右开区间写法

## 🧠 把螺旋矩阵的代码改为“左闭右开”的写法

我们以你之前的代码为基础，进行修改：

```python
class Solution:
    def generateMatrix(self, n: int) -> list[list[int]]:
        result = [[0] * n for _ in range(n)]
        cnt = 1

        for layer in range((n + 1) // 2):
            # 定义当前圈的边界：左闭右开
            start = layer
            end = n - layer

            # → 从左到右（上边）
            for ptr in range(start, end):
                result[start][ptr] = cnt
                cnt += 1

            # ↓ 从上到下（右边）
            for ptr in range(start + 1, end):
                result[ptr][end - 1] = cnt
                cnt += 1

            # ← 从右到左（下边）
            if end - 1 > start:  # 至少要有两行，才需要这一行
                for ptr in range(end - 2, start - 1, -1):
                    result[end - 1][ptr] = cnt
                    cnt += 1

            # ↑ 从下到上（左边）
            if end - 1 > start:  # 至少要有两列，才需要这一列
                for ptr in range(end - 2, start, -1):
                    result[ptr][start] = cnt
                    cnt += 1

        return result

```